<%= form_for(@acesso) do |f| %>
  <% if @acesso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acesso.errors.count, "error") %> prohibited this acesso from being saved:</h2>

      <ul>
      <% @acesso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_acesso %><br>
    <%= f.text_field :id_acesso %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :cliente_id %><br>
    <%= f.collection_select(:cliente_id, Cliente.all, :id, :nome, prompt: true) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
